import { Metadata } from "next"
import { generateMetadata } from "@/lib/metadata"

export const metadata: Metadata = generateMetadata({
  title: "Pricing",
  description: "Choose the perfect plan for your AI companion journey. From free starter plans to premium features with unlimited conversations and advanced personalization.",
})